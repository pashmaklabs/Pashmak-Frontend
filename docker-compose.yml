services:
  pashmak_frontend:
    image: registry.hamdocker.ir/pashm-narm/pashmak-frontend:1.0.0
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 80:80