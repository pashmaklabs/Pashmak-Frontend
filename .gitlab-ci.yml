# stages:
#   - build
#   - deploy

# check_branch_pipeline:
#   stage: build
#   script:
#     - echo "Branch pipeline executed (no tag)"
#   rules:
#     - if: '$CI_COMMIT_TAG == null'

# build_dev:
#   stage: build
#   image:
#     name: git.eduland.ir:5050/danio/front/executor
#     entrypoint: [""]
#   script:
#     - /kaniko/executor
#       --context "${CI_PROJECT_DIR}"
#       --dockerfile "${CI_PROJECT_DIR}/Dockerfile"
#       --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG}"
#       --build-arg ELASTIC_APM_SERVICE_VERSION=$CI_COMMIT_TAG
#   rules:
#     - if: '$CI_COMMIT_TAG =~ /.*-dev$/'

# build_prod:
#   stage: build
#   image:
#     name: git.eduland.ir:5050/danio/front/executor
#     entrypoint: [""]
#   script:
#     - /kaniko/executor
#       --context "${CI_PROJECT_DIR}"
#       --dockerfile "${CI_PROJECT_DIR}/Dockerfile"
#       --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG}"
#       --build-arg ELASTIC_APM_SERVICE_VERSION=$CI_COMMIT_TAG
#   rules:
#     - if: '$CI_COMMIT_TAG =~ /.*-prod$/'

# dev-deploy:
#   stage: deploy
#   variables:
#     DEPLOY_CURL_COMMAND: "curl -X PUT 'https://api.console.hamravesh.ir/api/v1/darkube/apps/update_from_cli/' -H 'Content-Type: application/json' -d '{\"trigger_deploy_token\": \"${DEPLOY_TOKEN_DANIO_BACK_DEV_DANIO_HAMRAVESH_C13}\", \"app_id\": \"${APP_ID_DANIO_BACK_DEV_DANIO_HAMRAVESH_C13}\", \"image_tag\": \"${CI_COMMIT_TAG}\"}'"
#   script:
#     - apk add --no-cache curl
#     - eval "$DEPLOY_CURL_COMMAND"
#   rules:
#     - if: '$CI_COMMIT_TAG =~ /.*-dev$/'

# prod-deploy:
#   stage: deploy
#   variables:
#     DEPLOY_CURL_COMMAND: "curl -X PUT 'https://api.console.hamravesh.ir/api/v1/darkube/apps/update_from_cli/' -H 'Content-Type: application/json' -d '{\"trigger_deploy_token\": \"${DEPLOY_TOKEN_BACKEND_PROD_DANIO_PROD_HAMRAVESH_C11}\", \"app_id\": \"${APP_ID_BACKEND_PROD_DANIO_PROD_HAMRAVESH_C11}\", \"image_tag\": \"${CI_COMMIT_TAG}\"}'"
#   script:
#     - apk add --no-cache curl
#     - eval "$DEPLOY_CURL_COMMAND"
#   rules:
#     - if: '$CI_COMMIT_TAG =~ /.*-prod$/'
